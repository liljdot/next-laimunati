import BuildComponent from "../components/BuildComponent";
const metadata = {
    title: "Build"
}

const Build = () => {
    return (
        <>
            < BuildComponent />
        </>
    )
}

export {metadata}
export default Build;